'use strict';

/**
 * Enum of url qualities in descending order of quality.
 * @type {Array}
 */
const DESC_QUALITIES = [
  'url_o', 'url_l', 'url_c', 'url_z', 'url_n'
];

module.exports = DESC_QUALITIES;
